node_modules/
logs/
probe.png
tiguans.json
.env
.env.*
.DS_Store
*.log
data/*.tmp.json
# The applet is a .app bundle (binary, code-signed at build time on the
# Mac running it). Rebuild locally with bin/build-applet.sh.
bin/ScrapeRunner.app/
